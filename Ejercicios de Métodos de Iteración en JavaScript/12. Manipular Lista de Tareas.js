const tasks = ['Wash dishes', 'Do laundry', 'Clean room'];

tasks.push('Buy groceries');     
tasks.unshift('Feed the cat');   
console.log(tasks);

tasks.shift(); 
tasks.pop();   
console.log(tasks); 
