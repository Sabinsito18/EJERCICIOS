const sentences = ['Hello world', 'JavaScript is fun', 'I love coding', 'Keep it simple'];

const frasesLargas = sentences.filter(sentence => sentence.length > 12);
console.log(frasesLargas); 

console.log(frasesLargas.length); 
